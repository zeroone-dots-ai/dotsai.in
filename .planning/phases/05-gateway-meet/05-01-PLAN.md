---
phase: 05-gateway-meet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/index.html
autonomous: true

must_haves:
  truths:
    - "Gateway section visible between Proof and Contact sections on dotsai.in"
    - "Three destination cards render: dotsai.cloud, zeroonedotsai.consulting, meet.dotsai.in"
    - "Each card is a clickable link that opens the destination in a new tab"
    - "Cards animate in on scroll with GSAP ScrollTrigger"
    - "Clicking a card fires a gateway_click analytics event"
    - "Analytics snippet is present and window.dotsTrack is available"
  artifacts:
    - path: "public/index.html"
      provides: "Gateway section HTML + CSS + GSAP animation + analytics snippet"
      contains: "id=\"gateway\""
  key_links:
    - from: "gateway card click"
      to: "window.dotsTrack('gateway_click', {destination: '...'})"
      via: "onclick handler on each card link"
      pattern: "dotsTrack.*gateway_click"
    - from: "analytics snippet"
      to: "https://api.dotsai.in/events"
      via: "fetch POST with Bearer token"
      pattern: "api\\.dotsai\\.in/events"
---

<objective>
Add a Gateway section to public/index.html with three destination cards and restore the analytics tracking snippet.

Purpose: Visitors can navigate to all hub destinations from one branded section. The analytics snippet (lost during SEO auto-optimisation runs) is restored so gateway_click events and page_view events flow to PostgreSQL.

Output: Updated public/index.html with Gateway section + working analytics.
</objective>

<execution_context>
@/Users/meetdeshani/.claude/get-shit-done/workflows/execute-plan.md
@/Users/meetdeshani/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-fastapi-analytics-api/02-04-SUMMARY.md
@public/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore analytics snippet + add Gateway section HTML/CSS/GSAP</name>
  <files>public/index.html</files>
  <action>
This task makes three changes to public/index.html:

**A. Restore the analytics tracking snippet (lost during SEO auto-runs).**

Insert before the closing `</style>` tag (same position as original commit 47579f6). The snippet is an IIFE that:
- Sets API = "https://api.dotsai.in/events"
- Sets TOKEN = "8ad6b8017639f8b248ccb25609f9112e14472e932169567948b554aa16a03b02"
- Defines track(name, props) using fetch with keepalive:true and Bearer auth
- Auto-fires track("page_view") on load
- Fires track("session_start") once per session via sessionStorage gate (_ds key)
- Exposes window.dotsTrack = track for manual event tracking

Place this `<script>` block AFTER the closing `</style>` tag and BEFORE the `</head>` tag.

**B. Add Gateway section HTML between Proof and Contact.**

Insert a new `<section id="gateway">` between `</section><!-- end #proof -->` and `<!-- --- CONTACT --- -->`. Structure:

```html
<!-- --- GATEWAY --- -->
<section id="gateway">
  <p class="gateway-label">// explore</p>
  <h2 class="gateway-title">The Hub</h2>
  <p class="gateway-sub">Three doors. One ecosystem.</p>
  <div class="gateway-cards">
    <a href="https://dotsai.cloud" target="_blank" rel="noopener" class="gateway-card" onclick="window.dotsTrack&&window.dotsTrack('gateway_click',{destination:'dotsai.cloud'})">
      <span class="gateway-card-dot" style="background: var(--dots-ops);"></span>
      <h3 class="gateway-card-name">Infinity Platform</h3>
      <p class="gateway-card-desc">Your private AI operating system. CRM, docs, automation — on your server.</p>
      <span class="gateway-card-url">dotsai.cloud <span class="gateway-arrow">↗</span></span>
    </a>
    <a href="https://zeroonedotsai.consulting" target="_blank" rel="noopener" class="gateway-card" onclick="window.dotsTrack&&window.dotsTrack('gateway_click',{destination:'zeroonedotsai.consulting'})">
      <span class="gateway-card-dot" style="background: var(--dots-strategy);"></span>
      <h3 class="gateway-card-name">Global Consulting</h3>
      <p class="gateway-card-desc">AI strategy and deployment for teams worldwide. Enterprise-grade, founder-delivered.</p>
      <span class="gateway-card-url">zeroonedotsai.consulting <span class="gateway-arrow">↗</span></span>
    </a>
    <a href="https://meet.dotsai.in" target="_blank" rel="noopener" class="gateway-card" onclick="window.dotsTrack&&window.dotsTrack('gateway_click',{destination:'meet.dotsai.in'})">
      <span class="gateway-card-dot" style="background: var(--dots-tech);"></span>
      <h3 class="gateway-card-name">Meet Deshani</h3>
      <p class="gateway-card-desc">The person behind ZeroOne. Book a call, see the work, start a conversation.</p>
      <span class="gateway-card-url">meet.dotsai.in <span class="gateway-arrow">↗</span></span>
    </a>
  </div>
</section>
```

**C. Add Gateway CSS in the `<style>` block.**

Insert after the Proof section styles (after `SECTION 4: PROOF` styles, before `SECTION 5: CONTACT`). Use this comment header: `/* --- SECTION 4b: GATEWAY --- */`

CSS requirements:
- `#gateway` background: var(--paper), padding 120px 8% 100px
- `#gateway::before` — same top-border pattern as other sections (1px solid var(--bone) at top)
- `.gateway-label` — Space Mono, 11px, uppercase, letter-spacing 2px, color var(--smoke), margin-bottom 24px
- `.gateway-title` — Instrument Serif, 3rem (clamp 2rem to 3.5rem), color var(--ink), margin-bottom 12px
- `.gateway-sub` — DM Sans, 1.1rem, color var(--smoke), margin-bottom 56px
- `.gateway-cards` — flex row, gap 24px, wrap on mobile (flex-direction: column below 768px)
- `.gateway-card` — flex: 1, min-width: 0, background var(--bg), border: 1px solid var(--bone), border-radius: 12px, padding: 36px 28px, text-decoration: none, color: var(--ink), display: flex, flex-direction: column, gap: 12px, min-height: 48px (tap target), transition: border-color 200ms ease-out, transform 200ms ease-out
- `.gateway-card:hover` — border-color: var(--plum), transform: translateY(-2px) (gate behind `@media (hover: hover) and (pointer: fine)`)
- `.gateway-card:active` — transform: scale(0.98) (per design engineering skill)
- `.gateway-card-dot` — 8px width, 8px height, border-radius: 50%, display: inline-block, margin-bottom: 8px
- `.gateway-card-name` — Instrument Serif, 1.35rem, color: var(--ink)
- `.gateway-card-desc` — DM Sans, 0.95rem, color: var(--smoke), line-height: 1.5, flex: 1
- `.gateway-card-url` — Space Mono, 0.75rem, color: var(--plum), margin-top: 16px, letter-spacing: 0.5px
- `.gateway-arrow` — display: inline-block, transition: transform 150ms ease-out
- `.gateway-card:hover .gateway-arrow` — transform: translate(2px, -2px) (inside the hover media query)
- Mobile: full-width cards, `.gateway-card` min-height: 44px for tap target compliance

NO bento grid. NO glassmorphism. NO neon. Simple stacked cards with plum accent on hover.

**D. Add GSAP ScrollTrigger animation for Gateway in `initMainPage()`.**

Insert AFTER the case cards animation block (after the `// -- Case cards --` section, around line 2302) and BEFORE the `// -- Contact fade-up --` section. Pattern matches existing sections:

```js
// -- Gateway cards fade-up --
gsap.to('.gateway-label', {
  opacity: 1, y: 0, duration: 0.6, ease: 'power3.out',
  scrollTrigger: { trigger: '#gateway', start: 'top 75%' }
})
gsap.to('.gateway-title', {
  opacity: 1, y: 0, duration: 0.7, ease: 'power3.out',
  scrollTrigger: { trigger: '#gateway', start: 'top 75%' }
})
gsap.to('.gateway-sub', {
  opacity: 1, y: 0, duration: 0.6, ease: 'power3.out', delay: 0.1,
  scrollTrigger: { trigger: '#gateway', start: 'top 75%' }
})
document.querySelectorAll('.gateway-card').forEach(function(card, i) {
  gsap.to(card, {
    opacity: 1, y: 0, duration: 0.6, ease: 'power3.out',
    delay: i * 0.08,
    scrollTrigger: { trigger: '.gateway-cards', start: 'top 82%' }
  })
})
```

Set initial state in CSS (same pattern as other sections): `.gateway-label, .gateway-title, .gateway-sub, .gateway-card { opacity: 0; transform: translateY(24px); }`

  </action>
  <verify>
1. Open public/index.html in browser — Gateway section visible between Proof and Contact
2. Three cards display with correct titles: "Infinity Platform", "Global Consulting", "Meet Deshani"
3. Each card links to correct URL (dotsai.cloud, zeroonedotsai.consulting, meet.dotsai.in)
4. Scroll to Gateway — cards animate in with stagger
5. Open DevTools console — type `typeof window.dotsTrack` — should return "function"
6. grep for "gateway_click" in public/index.html — 3 occurrences (one per card)
7. grep for "api.dotsai.in/events" — confirms analytics snippet is present
  </verify>
  <done>
- Gateway section renders 3 destination cards between Proof and Contact
- Cards animate in via GSAP ScrollTrigger on scroll
- Each card onclick fires window.dotsTrack('gateway_click', {destination: '...'})
- Analytics snippet restored: window.dotsTrack available, page_view fires on load
- Tap targets >= 44px on mobile (min-height on cards)
- Hover states gated behind @media (hover: hover) and (pointer: fine)
  </done>
</task>

</tasks>

<verification>
- `grep -c "id=\"gateway\"" public/index.html` returns 1
- `grep -c "gateway_click" public/index.html` returns 3
- `grep -c "dotsTrack" public/index.html` returns at least 5 (3 gateway + snippet definition + exposure)
- `grep -c "api.dotsai.in/events" public/index.html` returns 1
- Cards visible and animated when scrolling in browser
</verification>

<success_criteria>
Gateway section with 3 cards renders on dotsai.in between Proof and Contact. Analytics snippet present. Cards fire gateway_click events. Mobile tap targets >= 44px. No bento grid, no glassmorphism.
</success_criteria>

<output>
After completion, create `.planning/phases/05-gateway-meet/05-01-SUMMARY.md`
</output>
